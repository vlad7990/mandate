# Eval fixtures (slice 3, gate 03bafc3)

Exported once from the live demo mandates (2026-08-25) — real data,
sanitization = none needed (the workspace's own illustrative content).
Thin real state is exported as-is: zero boolean queries, empty
feedback/contact histories. Fixture counts are small (1–2 per
capability) and the results table says so.

`cvs/` is the FOUNDER's drop-point for real CV files (PDF/DOCX) —
gitignored; parse_cv's benchmark waits on it.
